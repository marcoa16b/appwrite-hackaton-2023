/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cloud.appwrite.io',
      'images.unsplash.com',
      'i.imgur.com'
    ]
  }
}

module.exports = nextConfig
